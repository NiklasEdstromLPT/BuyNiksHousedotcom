export const FLAGS = {
  ENABLE_Q4_STRUCTURAL: false,   // Stage 4 Q4 question — OFF per spec
  ENABLE_TWILIO_SMS: false,      // Stage 5 SMS verification — OFF per spec
  ENABLE_GOOGLE_PLACES: false,   // Stage 1 autocomplete — set true if API key available
};
